---
- name: Install common programs
  become: yes
  action: >
    {{ ansible_pkg_mgr }} name={{ item }} state=present update_cache=yes
  with_items:
  - libselinux-python
  - git
  - vlc
  - vim
  - tmux
  - nmap
