---
#- include: install.yml
- include_vars: "{{ lookup('first_found',params) }}"
  vars:
    params:
      files:
        - "{{ ansible_os_family }}.yml"
        - default.yml
      paths:
        - 'vars'
- include: latex_template.yml
  with_items:
    - {template_name: "dnd", repo_url: "https://github.com/rpgtex/DND-5e-LaTeX-Template.git"}
- include: clone_repo.yml
  with_items:
    - {directory: "DnD_LaTeX/adventures", repo_url: "https://github.com/decyne/dnd-adventures"}
    - {directory: "DnD_LaTeX/templates", repo_url: "https://github.com/decyne/dnd-adventure-templates"}
